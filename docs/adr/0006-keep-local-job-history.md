# Keep local job history

SplitFast will keep Job History indefinitely by default as metadata only: job status, timing, source name, clip count, and output asset identifiers. Users can delete individual entries or clear all history. This adds local persistence, but gives background splitting a recovery surface without keeping Source Video copies.
